import { JobList } from "@/components/Jobs/JobList";

const Job = () => {
  // call the backend as server side here then pass it as props to JobList component
  return <JobList />;
};

export default Job;
